#include<iostream>
using namespace std;
int main(){

/*inline function in inline function the code are replace at ponit 
of the function call but used to save time but cosume space */
/*static function used to increase the valuve of function constants and only once executed by the compiler*/
/*defalt argument is used to give value to const if user forget to give but if user give another value then other value accepted*/
return 0 ;
}